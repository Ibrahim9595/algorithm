/**
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
const combinationSum = (candidates, target, [arr, sum] = [[], 0]) => {
    
};

console.log(combinationSum([2, 3, 4, 5], 8));

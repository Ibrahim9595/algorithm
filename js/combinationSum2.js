//https://leetcode.com/problems/combination-sum-ii/
/**
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
const combinationSum2 = (candidates, target, [arr, sum] = [[], 0]) => {
    
};

console.log(combinationSum([2, 3, 4, 5], 8));
